const initialState = {
    menuPoints: [
        'Учетная запись',
        'Способы оплаты',
        'История',
        'Календарь'
    ]
};


export function aside(state = initialState) {
    return state;
}